{
  "$schema": "https://platformatic.dev/schemas/v1.53.3/runtime",
  "entrypoint": "platformatic-db-error",
  "allowCycles": false,
  "hotReload": true,
  "autoload": {
    "path": "services",
    "exclude": [
      "docs"
    ]
  },
  "server": {
    "hostname": "{PLT_SERVER_HOSTNAME}",
    "port": "{PORT}",
    "logger": {
      "level": "{PLT_SERVER_LOGGER_LEVEL}"
    }
  },
  "managementApi": "{PLT_MANAGEMENT_API}"
}